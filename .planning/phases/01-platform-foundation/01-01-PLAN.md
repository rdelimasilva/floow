---
phase: 01-platform-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - .npmrc
  - turbo.json
  - tsconfig.json
  - .gitignore
  - .env.example
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/globals.css
  - apps/web/tailwind.config.ts
  - apps/web/postcss.config.js
  - apps/mobile/package.json
  - apps/mobile/app/_layout.tsx
  - apps/mobile/tsconfig.json
  - packages/db/package.json
  - packages/db/tsconfig.json
  - packages/db/src/index.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/core-finance/package.json
  - packages/core-finance/tsconfig.json
  - packages/core-finance/src/index.ts
  - apps/web/components/ui/button.tsx
  - apps/web/components/ui/input.tsx
  - apps/web/components/ui/card.tsx
  - apps/web/components/ui/tabs.tsx
  - apps/web/components/ui/form.tsx
  - apps/web/components/ui/label.tsx
  - apps/web/lib/utils.ts
  - playwright.config.ts
autonomous: true
requirements:
  - AUTH-05

must_haves:
  truths:
    - "pnpm install succeeds without errors at monorepo root"
    - "turbo run build completes for all packages"
    - "Next.js dev server starts and renders a page"
    - "All @floow/* packages resolve correctly via workspace protocol"
  artifacts:
    - path: "package.json"
      provides: "Root monorepo package.json with turbo and typescript devDeps"
    - path: "pnpm-workspace.yaml"
      provides: "Workspace definition for apps/* and packages/*"
    - path: ".npmrc"
      provides: "node-linker=hoisted for Expo compatibility"
    - path: "turbo.json"
      provides: "Task pipeline (build, dev, lint, typecheck, db:generate, db:migrate)"
    - path: "apps/web/package.json"
      provides: "Next.js 16 app with Supabase, RHF, Zod, TanStack Query, Stripe deps"
    - path: "packages/db/package.json"
      provides: "@floow/db package with Drizzle ORM + drizzle-kit"
    - path: "packages/shared/package.json"
      provides: "@floow/shared package with Zod schemas"
  key_links:
    - from: "apps/web/package.json"
      to: "packages/db"
      via: "workspace:* dependency"
      pattern: '"@floow/db":\\s*"workspace:\\*"'
    - from: "apps/web/package.json"
      to: "packages/shared"
      via: "workspace:* dependency"
      pattern: '"@floow/shared":\\s*"workspace:\\*"'
    - from: "turbo.json"
      to: "all packages"
      via: "tasks pipeline with ^build"
      pattern: '"dependsOn":\\s*\\["\\^build"\\]'
---

<objective>
Scaffold the Floow monorepo with Turborepo, pnpm workspaces, and all app/package stubs.

Purpose: Establish the foundational directory structure, package boundaries, and build pipeline that every subsequent plan depends on. Without this, no code has a home.

Output: A working monorepo where `pnpm install` resolves all workspaces, `turbo run build` succeeds, and the Next.js web app starts with a placeholder page.
</objective>

<execution_context>
@C:/Users/rdeli/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rdeli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation/01-CONTEXT.md
@.planning/phases/01-platform-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo root configuration files and web app</name>
  <files>
    package.json, pnpm-workspace.yaml, .npmrc, turbo.json, tsconfig.json, .gitignore, .env.example,
    apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json,
    apps/web/app/layout.tsx, apps/web/app/page.tsx, apps/web/app/globals.css,
    apps/web/tailwind.config.ts, apps/web/postcss.config.js
  </files>
  <action>
    Create the root monorepo configuration and the web app (15 files).

    **Root files:**
    - `package.json`: name "floow", private true, devDependencies: turbo ^2.8, typescript ^5.7. Scripts: "dev": "turbo dev", "build": "turbo build", "lint": "turbo lint", "typecheck": "turbo typecheck".
    - `pnpm-workspace.yaml`: packages: ['apps/*', 'packages/*']. Add catalog with pinned versions for typescript ^5.7, zod ^3.24, @tanstack/react-query ^5.67, react ^19.0, react-dom ^19.0.
    - `.npmrc`: node-linker=hoisted (REQUIRED for Expo — pnpm isolated mode breaks React Native native builds).
    - `turbo.json`: Use "tasks" key (NOT deprecated "pipeline"). Tasks: build (dependsOn: ["^build"], outputs: [".next/**", "!.next/cache/**", "dist/**"]), dev (cache: false, persistent: true), lint (dependsOn: ["^lint"]), typecheck (dependsOn: ["^build"]), test (dependsOn: ["^build"], cache: false), db:generate (cache: false), db:migrate (cache: false).
    - `tsconfig.json`: Base config with strict mode, target ES2022, moduleResolution bundler, paths for @floow/* packages.
    - `.gitignore`: node_modules, .next, dist, .turbo, .env, .env.local, *.tsbuildinfo, coverage, .expo, test-results, playwright-report.
    - `.env.example`: Document all env vars needed: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL, STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET, EXPO_PUBLIC_SUPABASE_URL, EXPO_PUBLIC_SUPABASE_ANON_KEY.

    **apps/web (Next.js 16):**
    - `package.json`: name "@floow/web", dependencies: next ^16.1, react ^19, react-dom ^19, @supabase/supabase-js ^2, @supabase/ssr ^0.9, react-hook-form ^7, @hookform/resolvers ^3, zod (catalog:), @tanstack/react-query (catalog:), stripe ^20, @stripe/stripe-js ^4. devDependencies: typescript (catalog:), tailwindcss ^3, postcss, autoprefixer, @types/react, @types/node. Workspace deps: "@floow/db": "workspace:*", "@floow/shared": "workspace:*". Scripts: dev, build, start, lint, test.
    - `next.config.ts`: transpilePackages: ['@floow/db', '@floow/shared', '@floow/core-finance'].
    - `tsconfig.json`: extends root, paths: {"@/*": ["./src/*"], "@floow/db": ["../../packages/db/src"], "@floow/shared": ["../../packages/shared/src"]}.
    - `tailwind.config.ts`: content paths including ./app/**/*, ./components/**/*. Standard config.
    - `postcss.config.js`: tailwindcss + autoprefixer.
    - `app/layout.tsx`: Root layout with html/body, import globals.css with Tailwind directives.
    - `app/page.tsx`: Simple placeholder page with "Floow" heading and "Platform loading..." text. No business logic.
    - `app/globals.css`: Tailwind directives (@tailwind base/components/utilities).

    Do NOT create apps/functions-netlify or apps/functions-supabase yet — those are not needed until billing webhooks (Plan 01-04).
  </action>
  <verify>
    <automated>pnpm install && pnpm turbo run build --filter=@floow/web</automated>
  </verify>
  <done>
    - pnpm install resolves all workspace dependencies without errors
    - turbo run build completes for @floow/web
    - Root config files are correct (turbo.json uses "tasks", .npmrc has node-linker=hoisted)
    - .env.example documents all required env vars
  </done>
</task>

<task type="auto">
  <name>Task 2: Create app/package stubs for mobile, db, shared, and core-finance</name>
  <files>
    apps/mobile/package.json, apps/mobile/app/_layout.tsx, apps/mobile/tsconfig.json,
    packages/db/package.json, packages/db/tsconfig.json, packages/db/src/index.ts,
    packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts,
    packages/core-finance/package.json, packages/core-finance/tsconfig.json, packages/core-finance/src/index.ts
  </files>
  <action>
    Create the remaining app and package stubs (12 files).

    **apps/mobile (Expo SDK 55):**
    - `package.json`: name "@floow/mobile", dependencies: expo ~55, react-native, @supabase/supabase-js ^2, @react-native-async-storage/async-storage ^2, react-native-url-polyfill ^2, expo-linking ~7, expo-web-browser ~14. Workspace deps: "@floow/shared": "workspace:*". Scripts: start, android, ios, web.
    - `tsconfig.json`: extends expo default.
    - `app/_layout.tsx`: Expo Router root layout with basic Stack navigator placeholder.

    **packages/db (@floow/db):**
    - `package.json`: name "@floow/db", main "./src/index.ts", types "./src/index.ts". Dependencies: drizzle-orm ^0.45, postgres (pg driver). devDependencies: drizzle-kit. Scripts: "generate": "drizzle-kit generate", "migrate": "drizzle-kit migrate", "studio": "drizzle-kit studio", "test": "vitest run".
    - `tsconfig.json`: extends root.
    - `src/index.ts`: Empty barrel export with comment "// Schema and client exports will be added in Plan 01-02".

    **packages/shared (@floow/shared):**
    - `package.json`: name "@floow/shared", main "./src/index.ts". Dependencies: zod (catalog:).
    - `tsconfig.json`: extends root.
    - `src/index.ts`: Empty barrel export with comment "// Shared types, Zod schemas, and utils".

    **packages/core-finance (@floow/core-finance):**
    - `package.json`: name "@floow/core-finance", main "./src/index.ts". Dependencies: zod (catalog:).
    - `tsconfig.json`: extends root.
    - `src/index.ts`: Empty barrel export with comment "// Core finance engine — populated in Phase 2".

    After creating all stubs, run `pnpm install` again to resolve the newly added workspace packages.
  </action>
  <verify>
    <automated>pnpm install && pnpm turbo run build</automated>
  </verify>
  <done>
    - All @floow/* packages exist with valid package.json
    - pnpm install resolves all workspace packages
    - turbo run build succeeds across the entire monorepo
  </done>
</task>

<task type="auto">
  <name>Task 3: Git init, shadcn/ui setup, Vitest + Playwright infrastructure</name>
  <files>
    apps/web/components/ui/button.tsx,
    apps/web/components/ui/input.tsx,
    apps/web/components/ui/card.tsx,
    apps/web/components/ui/tabs.tsx,
    apps/web/components/ui/form.tsx,
    apps/web/components/ui/label.tsx,
    apps/web/lib/utils.ts,
    playwright.config.ts
  </files>
  <action>
    **Git init:**
    - Initialize git repo at monorepo root: `git init`
    - Create initial commit with all scaffold files

    **shadcn/ui setup (web only):**
    - Initialize shadcn/ui in apps/web: `pnpm --filter @floow/web dlx shadcn@latest init` (accept defaults: New York style, zinc color, CSS variables yes)
    - Install components needed for auth page: Button, Input, Card, Tabs, Form, Label
    - Run: `pnpm --filter @floow/web dlx shadcn@latest add button input card tabs form label`
    - Verify components installed in apps/web/components/ui/
    - Create apps/web/lib/utils.ts with the cn() utility (clsx + tailwind-merge) if shadcn init doesn't create it

    **Vitest setup (Wave 0 test infrastructure):**
    - Add vitest and @vitest/coverage-v8 as devDependencies to root: `pnpm add -D vitest @vitest/coverage-v8`
    - Create vitest.config.ts in packages/db with basic config (test files in src/__tests__/)
    - Create vitest.config.ts in apps/web with basic config (test files in __tests__/)
    - Add "test" script to root package.json turbo tasks (already included in Task 1 turbo.json)
    - Add "test" script to apps/web/package.json: "test": "vitest run"

    **Playwright setup (Wave 0 e2e infrastructure):**
    - Install Playwright: `pnpm add -D @playwright/test`
    - Create `playwright.config.ts` at monorepo root with:
      - testDir: './e2e' (or 'apps/web/e2e')
      - baseURL: 'http://localhost:3000'
      - webServer config to start Next.js dev server
      - Projects: chromium only for now (keep fast)
    - This satisfies the Wave 0 requirement from VALIDATION.md

    **Full build verification:**
    - Run `pnpm turbo run build` for entire monorepo
    - Run `pnpm turbo run typecheck` to verify TypeScript config is correct
    - Verify Next.js dev server starts: `pnpm --filter @floow/web dev` (start and kill after confirming port binding)
  </action>
  <verify>
    <automated>pnpm turbo run build && pnpm turbo run typecheck</automated>
  </verify>
  <done>
    - Git repo initialized with initial commit
    - shadcn/ui components (Button, Input, Card, Tabs, Form, Label) exist in apps/web/components/ui/
    - cn() utility available in apps/web/lib/utils.ts
    - Vitest config exists in packages/db and apps/web
    - playwright.config.ts exists at monorepo root with baseURL localhost:3000
    - Full monorepo build and typecheck pass
  </done>
</task>

</tasks>

<verification>
- `pnpm install` exits 0
- `pnpm turbo run build` exits 0 (all packages build)
- `pnpm turbo run typecheck` exits 0
- `ls apps/web/components/ui/button.tsx` exists
- `ls packages/db/src/index.ts` exists
- `ls packages/shared/src/index.ts` exists
- `ls packages/core-finance/src/index.ts` exists
- `ls playwright.config.ts` exists
- `.npmrc` contains `node-linker=hoisted`
</verification>

<success_criteria>
A fully working monorepo where all packages build, the web app starts, shadcn/ui components are available, Vitest and Playwright test infrastructure are initialized. This is the foundation every subsequent plan builds upon.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation/01-01-SUMMARY.md`
</output>
