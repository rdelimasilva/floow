---
phase: 02-finance-engine
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/core-finance/src/import/ofx.ts
  - packages/core-finance/src/import/csv.ts
  - packages/core-finance/src/cash-flow.ts
  - packages/core-finance/src/index.ts
  - packages/core-finance/src/__tests__/import-ofx.test.ts
  - packages/core-finance/src/__tests__/import-csv.test.ts
  - packages/core-finance/src/__tests__/cash-flow.test.ts
  - apps/web/app/(app)/transactions/import/page.tsx
  - apps/web/components/finance/import-form.tsx
  - apps/web/components/finance/cash-flow-chart.tsx
  - apps/web/lib/finance/import-actions.ts
autonomous: true
requirements: [FIN-04, FIN-05]

must_haves:
  truths:
    - "OFX file parsing correctly extracts transactions with FITID, date, amount, and description"
    - "OFX dates in YYYYMMDDHHMMSS format are parsed correctly (not treated as ISO 8601)"
    - "CSV file parsing handles header mapping for Brazilian bank formats"
    - "Duplicate imports are prevented via external_id deduplication"
    - "Cash flow aggregation returns monthly totals grouped by category and type"
    - "User can upload an OFX or CSV file and see a preview before confirming import"
  artifacts:
    - path: "packages/core-finance/src/import/ofx.ts"
      provides: "OFX parser with FITID extraction and OFX date handling"
      exports: ["parseOFXFile", "parseOFXDate"]
    - path: "packages/core-finance/src/import/csv.ts"
      provides: "CSV parser with column mapping for Brazilian bank formats"
      exports: ["parseCSVFile", "CsvColumnMapping"]
    - path: "packages/core-finance/src/cash-flow.ts"
      provides: "Cash flow aggregation logic for monthly income/expense breakdown"
      exports: ["aggregateCashFlow"]
    - path: "apps/web/app/(app)/transactions/import/page.tsx"
      provides: "Import page with file upload and preview table"
    - path: "apps/web/lib/finance/import-actions.ts"
      provides: "Server action for importing parsed transactions with deduplication"
      exports: ["importTransactions"]
  key_links:
    - from: "packages/core-finance/src/import/ofx.ts"
      to: "ofx-js"
      via: "import { parse } from 'ofx-js'"
      pattern: "parseOFX"
    - from: "packages/core-finance/src/import/csv.ts"
      to: "papaparse"
      via: "import Papa from 'papaparse'"
      pattern: "Papa\\.parse"
    - from: "apps/web/lib/finance/import-actions.ts"
      to: "packages/core-finance/src/import/ofx.ts"
      via: "import { parseOFXFile } from '@floow/core-finance'"
      pattern: "parseOFXFile"
---

<objective>
Build OFX/CSV import parsers in core-finance and the cash flow aggregation logic, plus the import UI page with file upload and preview.

Purpose: Import capability (FIN-05) lets users populate transaction history from bank exports instead of manual entry. Cash flow aggregation (FIN-04) is the core financial insight computation used by the dashboard.
Output: Tested OFX/CSV parsers, cash flow aggregation function, import page with preview, server action with dedup.
</objective>

<execution_context>
@C:/Users/rdeli/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rdeli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-finance-engine/02-RESEARCH.md
@.planning/phases/02-finance-engine/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 -->

From packages/core-finance/src/types.ts:
```typescript
export interface NormalizedTransaction {
  externalId: string
  date: Date
  amountCents: number
  description: string
  type: 'income' | 'expense'
}

export interface CashFlowMonth {
  month: string
  income: number
  expense: number
  net: number
}
```

From packages/db/src/schema/finance.ts:
```typescript
export const transactions = pgTable('transactions', {
  id, orgId, accountId, categoryId, type, amountCents, description, date,
  transferGroupId, importedAt, externalId, createdAt
})
export type NewTransaction = typeof transactions.$inferInsert
```

From packages/core-finance/src/balance.ts:
```typescript
export function centsToCurrency(cents: number): string
export function formatBRL(cents: number): string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build OFX parser, CSV parser, and cash flow aggregation with tests</name>
  <files>
    packages/core-finance/src/import/ofx.ts,
    packages/core-finance/src/import/csv.ts,
    packages/core-finance/src/cash-flow.ts,
    packages/core-finance/src/index.ts,
    packages/core-finance/src/__tests__/import-ofx.test.ts,
    packages/core-finance/src/__tests__/import-csv.test.ts,
    packages/core-finance/src/__tests__/cash-flow.test.ts
  </files>
  <behavior>
    OFX Parser:
    - Test: parseOFXDate('20240115120000.000[-3:BRT]') returns Date for 2024-01-15
    - Test: parseOFXDate('20240115') returns Date for 2024-01-15
    - Test: parseOFXFile with valid OFX content returns array of NormalizedTransaction with correct externalId (FITID), date, amountCents, description, type
    - Test: parseOFXFile with debit transaction (negative TRNAMT) returns type='expense' with negative amountCents
    - Test: parseOFXFile with credit transaction (positive TRNAMT) returns type='income' with positive amountCents
    - Test: parseOFXFile with invalid content throws descriptive error

    CSV Parser:
    - Test: parseCSVFile with header mapping returns NormalizedTransaction array
    - Test: parseCSVFile handles "dd/MM/yyyy" date format correctly
    - Test: parseCSVFile handles negative amounts as expense
    - Test: parseCSVFile skips empty lines
    - Test: parseCSVFile generates deterministic externalId for deduplication

    Cash Flow:
    - Test: aggregateCashFlow groups transactions by month (YYYY-MM format)
    - Test: aggregateCashFlow separates income and expense totals
    - Test: aggregateCashFlow calculates net (income + expense where expense is negative)
    - Test: aggregateCashFlow with empty array returns empty array
    - Test: aggregateCashFlow sorts months descending (most recent first)
  </behavior>
  <action>
    1. Install new dependencies from monorepo root:
       ```
       pnpm add ofx-js papaparse --filter @floow/core-finance
       pnpm add -D @types/papaparse --filter @floow/core-finance
       ```

    2. Create test files FIRST (RED phase):
       - `packages/core-finance/src/__tests__/import-ofx.test.ts`: Tests for parseOFXDate and parseOFXFile. Use inline OFX content strings as test fixtures (sample SGML from 02-RESEARCH.md). Test FITID extraction, date parsing (both long and short formats), amount sign detection, error handling.
       - `packages/core-finance/src/__tests__/import-csv.test.ts`: Tests for parseCSVFile with CsvColumnMapping. Use inline CSV strings. Test date formats (dd/MM/yyyy and yyyy-MM-dd), amount parsing, empty line handling, deterministic externalId generation.
       - `packages/core-finance/src/__tests__/cash-flow.test.ts`: Tests for aggregateCashFlow. Create mock transaction arrays with known dates/amounts, verify month grouping, income/expense separation, net calculation, sort order.

    3. Run tests to confirm RED.

    4. Create `packages/core-finance/src/import/ofx.ts`:
       - parseOFXDate(ofxDate: string): Date — slices YYYYMMDD from the OFX date format, constructs ISO date string. CRITICAL: Do NOT pass raw OFX date to `new Date()` — it's not ISO 8601 (Pitfall 2 from research).
       - parseOFXFile(content: string): Promise<NormalizedTransaction[]> — uses ofx-js parse(), navigates OFX.BANKMSGSRSV1.STMTTRNRS.STMTRS.BANKTRANLIST.STMTTRN, maps each transaction using parseOFXDate for dates and Math.round(parseFloat(TRNAMT) * 100) for amountCents. Uses FITID as externalId. Sets type based on TRNAMT sign.

    5. Create `packages/core-finance/src/import/csv.ts`:
       - CsvColumnMapping interface: { dateColumn, amountColumn, descriptionColumn, dateFormat? }
       - parseCSVFile(content: string, mapping: CsvColumnMapping): NormalizedTransaction[] — uses Papa.parse with { header: true, skipEmptyLines: true }, maps each row using the column mapping. Generates externalId from a hash of the row content (Buffer.from(JSON.stringify(row)).toString('base64').slice(0, 24) prefixed with 'csv-'). Handles date format detection. Sets type from amount sign.

    6. Create `packages/core-finance/src/cash-flow.ts`:
       - aggregateCashFlow(transactions: Array<{ date: Date, amountCents: number, type: string }>): CashFlowMonth[] — groups by YYYY-MM, sums income (positive amounts where type=income) and expense (negative amounts where type=expense), calculates net, sorts descending by month.

    7. Update `packages/core-finance/src/index.ts` to export:
       ```
       export * from './balance'
       export * from './types'
       export * from './import/ofx'
       export * from './import/csv'
       export * from './cash-flow'
       ```

    8. Run tests (GREEN): `pnpm --filter @floow/core-finance test`
  </action>
  <verify>
    <automated>cd "C:/Users/rdeli/OneDrive/Área de Trabalho/floow" && pnpm --filter @floow/core-finance test</automated>
  </verify>
  <done>OFX parser handles SGML content, OFX date format, and FITID extraction. CSV parser handles column mapping and Brazilian date formats. Cash flow aggregation groups by month correctly. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build import page UI with file upload, preview, and server action</name>
  <files>
    apps/web/app/(app)/transactions/import/page.tsx,
    apps/web/components/finance/import-form.tsx,
    apps/web/components/finance/cash-flow-chart.tsx,
    apps/web/lib/finance/import-actions.ts
  </files>
  <action>
    1. Create `apps/web/lib/finance/import-actions.ts`:
       - 'use server'
       - importTransactions(formData: FormData): server action that:
         a. Gets orgId and accountId from formData
         b. Reads file content via (formData.get('file') as File).text()
         c. Detects format from file extension (.ofx vs .csv)
         d. For OFX: calls parseOFXFile(content) from @floow/core-finance
         e. For CSV: reads column mapping from formData fields (dateColumn, amountColumn, descriptionColumn, dateFormat), calls parseCSVFile(content, mapping)
         f. Inserts normalized transactions into DB with:
            - orgId, accountId from form
            - importedAt = new Date()
            - externalId from normalized transaction
            - Use onConflictDoNothing on externalId for dedup (Pitfall: Duplicate imports, research)
         g. After insert, update account balance atomically by summing all imported amounts: sql`balance_cents + ${totalDelta}`
         h. revalidatePath('/transactions') and revalidatePath('/accounts')
         i. Return { imported: count, skipped: duplicateCount }

    2. Create `apps/web/components/finance/import-form.tsx`:
       - Client component ('use client')
       - Props: accounts (Account[]) for account selection
       - State machine: 'select-file' -> 'preview' -> 'importing' -> 'done'
       - Step 1 (select-file): Account select dropdown + file input (.ofx, .csv). On file select, read and parse client-side for preview (import parsers from @floow/core-finance).
       - Step 2 (preview): Show parsed transactions in a table (date, description, amount, type). Show count. For CSV: show column mapping dropdowns (auto-detect common headers like 'Data', 'Valor', 'Descricao', 'Date', 'Amount', 'Description'). User can adjust mapping.
       - Step 3 (importing): Submit button calls importTransactions server action with FormData containing file + accountId + mapping. Show loading state.
       - Step 4 (done): Show result (X imported, Y skipped as duplicates). Link to /transactions.
       - Uses shadcn Card, Button, Select, Input, Table (install Table component: `pnpm dlx shadcn@latest add table`)

    3. Create `apps/web/app/(app)/transactions/import/page.tsx`:
       - React Server Component
       - Loads accounts via getAccounts(orgId) for the account select
       - Renders ImportForm with accounts prop
       - Page title: "Importar Transacoes"
       - Back link to /transactions

    4. Create `apps/web/components/finance/cash-flow-chart.tsx`:
       - Client component ('use client')
       - Props: data (CashFlowMonth[])
       - Uses shadcn ChartContainer + Recharts BarChart (from research Pattern 9)
       - Install chart component: `pnpm dlx shadcn@latest add chart` in apps/web
       - Chart config: income = green (#16a34a), expense = red (#dc2626)
       - BarChart with XAxis (month), two Bar elements (income, expense)
       - ChartTooltip with ChartTooltipContent for hover details
       - className="min-h-[300px] w-full" on ChartContainer (Pitfall 7 from research — zero height without min-h)
       - This component is used by the dashboard in Plan 04 and can be previewed at /transactions if desired

    5. Verify build: `pnpm turbo run build --filter=@floow/web`
  </action>
  <verify>
    <automated>cd "C:/Users/rdeli/OneDrive/Área de Trabalho/floow" && pnpm turbo run build --filter=@floow/web</automated>
  </verify>
  <done>Import page at /transactions/import shows file upload with account selection. OFX and CSV files can be uploaded, previewed, and imported. Duplicate imports are skipped. CashFlowChart component ready for dashboard. Build passes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @floow/core-finance test` passes (OFX, CSV, cash flow tests)
- `pnpm turbo run build --filter=@floow/web` passes
- Import page renders at /transactions/import
- CashFlowChart component exists and is importable
- parseOFXFile handles OFX date format (YYYYMMDDHHMMSS) correctly
- parseCSVFile supports column mapping for Brazilian bank formats
- Server action uses onConflictDoNothing for deduplication on externalId
</verification>

<success_criteria>
- OFX parser extracts FITID, parses OFX dates, handles positive/negative amounts
- CSV parser handles column mapping and Brazilian date formats
- Cash flow aggregation groups transactions by month with income/expense separation
- Import page allows file upload, shows preview, imports with dedup
- All core-finance tests pass, web build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-finance-engine/02-03-SUMMARY.md`
</output>
