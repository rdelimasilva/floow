---
phase: 02-finance-engine
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - packages/core-finance/src/snapshot.ts
  - packages/core-finance/src/__tests__/snapshot.test.ts
  - packages/core-finance/src/index.ts
  - apps/web/app/(app)/dashboard/page.tsx
  - apps/web/components/finance/patrimony-summary.tsx
  - apps/web/__tests__/finance/dashboard.test.tsx
  - apps/web/lib/finance/queries.ts
  - apps/web/lib/finance/actions.ts
autonomous: false
requirements: [DASH-01, VAL-01]

must_haves:
  truths:
    - "Dashboard shows account summary with total balance across all accounts"
    - "Dashboard shows cash flow chart for recent months"
    - "Dashboard shows quick stats (total income, total expenses, net for current month)"
    - "System can compute a patrimony snapshot from current account balances"
    - "Patrimony snapshot captures net worth, liquid assets, liabilities, and breakdown by account type"
    - "User can trigger a snapshot refresh from the dashboard"
  artifacts:
    - path: "packages/core-finance/src/snapshot.ts"
      provides: "computePatrimonySnapshot function"
      exports: ["computePatrimonySnapshot"]
    - path: "apps/web/app/(app)/dashboard/page.tsx"
      provides: "Financial dashboard with account summary, cash flow chart, patrimony snapshot"
    - path: "apps/web/components/finance/patrimony-summary.tsx"
      provides: "Patrimony summary card showing net worth, liquid assets, liabilities"
    - path: "apps/web/__tests__/finance/dashboard.test.tsx"
      provides: "RTL tests verifying dashboard renders account cards, stats, and CashFlowChart"
      contains: "render"
  key_links:
    - from: "apps/web/app/(app)/dashboard/page.tsx"
      to: "apps/web/lib/finance/queries.ts"
      via: "RSC data fetching: getAccounts, getTransactions, getLatestSnapshot"
      pattern: "getAccounts|getTransactions|getLatestSnapshot"
    - from: "apps/web/app/(app)/dashboard/page.tsx"
      to: "apps/web/components/finance/cash-flow-chart.tsx"
      via: "import CashFlowChart, pass aggregated data"
      pattern: "CashFlowChart"
    - from: "packages/core-finance/src/snapshot.ts"
      to: "packages/db/src/schema/finance.ts"
      via: "reads accounts, writes patrimonySnapshots"
      pattern: "accounts.*patrimonySnapshots"
---

<objective>
Build the financial dashboard (DASH-01) and patrimony snapshot engine (VAL-01). The dashboard consolidates account balances, cash flow charts, and patrimony status into a single overview page.

Purpose: The dashboard is the first thing users see after login — it must display the full financial picture at a glance. Patrimony snapshots capture point-in-time net worth for historical tracking.
Output: Complete dashboard page with RTL tests, patrimony snapshot computation, snapshot refresh action.
</objective>

<execution_context>
@C:/Users/rdeli/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rdeli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-finance-engine/02-RESEARCH.md
@.planning/phases/02-finance-engine/02-01-SUMMARY.md
@.planning/phases/02-finance-engine/02-02-SUMMARY.md
@.planning/phases/02-finance-engine/02-03-SUMMARY.md

<interfaces>
<!-- From Plans 02-01, 02-02, 02-03 -->

From packages/db/src/schema/finance.ts:
```typescript
export const accounts = pgTable('accounts', { id, orgId, name, type, balanceCents, currency, isActive, ... })
export const transactions = pgTable('transactions', { id, orgId, accountId, categoryId, type, amountCents, description, date, ... })
export const patrimonySnapshots = pgTable('patrimony_snapshots', { id, orgId, snapshotDate, netWorthCents, liquidAssetsCents, liabilitiesCents, breakdown, ... })
export type Account = typeof accounts.$inferSelect
export type PatrimonySnapshot = typeof patrimonySnapshots.$inferSelect
export type NewPatrimonySnapshot = typeof patrimonySnapshots.$inferInsert
```

From packages/core-finance/src/balance.ts:
```typescript
export function formatBRL(cents: number): string
```

From packages/core-finance/src/cash-flow.ts:
```typescript
export function aggregateCashFlow(transactions: Array<{ date: Date, amountCents: number, type: string }>): CashFlowMonth[]
```

From apps/web/components/finance/cash-flow-chart.tsx:
```typescript
export function CashFlowChart({ data }: { data: CashFlowMonth[] }): JSX.Element
```

From apps/web/lib/finance/queries.ts:
```typescript
export async function getAccounts(orgId: string): Promise<Account[]>
export async function getTransactions(orgId: string, opts?): Promise<Transaction[]>
export async function getCategories(orgId: string): Promise<Category[]>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build patrimony snapshot computation with tests</name>
  <files>
    packages/core-finance/src/snapshot.ts,
    packages/core-finance/src/__tests__/snapshot.test.ts,
    packages/core-finance/src/index.ts
  </files>
  <behavior>
    - Test: computeSnapshot with 2 checking accounts (10000, 5000) returns netWorthCents=15000, liquidAssetsCents=15000, liabilitiesCents=0
    - Test: computeSnapshot with credit_card account (-3000) counts it as liability, reduces net worth
    - Test: computeSnapshot with mixed accounts (checking 10000, savings 20000, credit_card -5000) returns netWorthCents=25000, liquidAssetsCents=30000, liabilitiesCents=5000
    - Test: computeSnapshot generates breakdown object with per-type totals: { checking: 10000, savings: 20000 }
    - Test: computeSnapshot with no active accounts returns all zeros
    - Test: computeSnapshot sets snapshotDate to current time
  </behavior>
  <action>
    1. Create `packages/core-finance/src/__tests__/snapshot.test.ts`:
       - Test computeSnapshot (pure function version that takes Account[] array, NOT the DB-connected version)
       - Create mock Account arrays with different type/balance combinations
       - Verify netWorthCents, liquidAssetsCents, liabilitiesCents, breakdown calculations
       - Run (RED)

    2. Create `packages/core-finance/src/snapshot.ts`:
       - Import Account type from @floow/db, NewPatrimonySnapshot type
       - computeSnapshot(accounts: Account[], orgId: string): NewPatrimonySnapshot — pure function (no DB):
         - Iterate accounts, sum by type into breakdown object
         - totalCents = sum of all balanceCents
         - credit_card type → liabilities (absolute value)
         - everything else → liquid assets
         - netWorthCents = liquidAssetsCents - liabilitiesCents
         - Return { orgId, snapshotDate: new Date(), netWorthCents, liquidAssetsCents, liabilitiesCents, breakdown: JSON.stringify(breakdown) }

       - computeAndSaveSnapshot(db, orgId): async function that:
         - Fetches active accounts from DB
         - Calls computeSnapshot
         - Inserts into patrimonySnapshots table
         - Returns the saved snapshot

    3. Update `packages/core-finance/src/index.ts` to add: `export * from './snapshot'`

    4. Run tests (GREEN): `pnpm --filter @floow/core-finance test`
  </action>
  <verify>
    <automated>cd "C:/Users/rdeli/OneDrive/Área de Trabalho/floow" && pnpm --filter @floow/core-finance test</automated>
  </verify>
  <done>computeSnapshot correctly calculates net worth, liquid assets, liabilities, and breakdown from account data. All snapshot tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build financial dashboard page with account summary, cash flow chart, patrimony display, and RTL tests</name>
  <files>
    apps/web/app/(app)/dashboard/page.tsx,
    apps/web/components/finance/patrimony-summary.tsx,
    apps/web/__tests__/finance/dashboard.test.tsx,
    apps/web/lib/finance/queries.ts,
    apps/web/lib/finance/actions.ts
  </files>
  <behavior>
    - Test: Dashboard renders account cards with name and formatted balance for each account
    - Test: Dashboard renders quick stats section with "Receitas do Mes", "Despesas do Mes", "Saldo do Mes"
    - Test: Dashboard renders CashFlowChart component when transaction data exists
    - Test: Dashboard renders PatrimonySummary component with net worth display
    - Test: Dashboard renders empty state CTAs when no accounts exist
  </behavior>
  <action>
    1. Add query helpers to `apps/web/lib/finance/queries.ts`:
       - getLatestSnapshot(orgId: string): select from patrimonySnapshots where orgId matches, order by snapshotDate DESC, limit 1
       - getRecentTransactions(orgId: string, months: number = 6): select transactions from last N months for cash flow chart

    2. Add server action to `apps/web/lib/finance/actions.ts`:
       - refreshSnapshot(): server action that calls computeAndSaveSnapshot(db, orgId), revalidatePath('/dashboard')

    3. Create `apps/web/components/finance/patrimony-summary.tsx`:
       - Client component
       - Props: snapshot (PatrimonySnapshot | null), onRefresh (server action reference)
       - Displays: net worth (large, bold, formatted BRL), liquid assets, liabilities — each with formatBRL
       - Breakdown section: shows per-account-type amounts parsed from snapshot.breakdown JSON
       - "Update Snapshot" button that calls onRefresh server action
       - If snapshot is null: show "No snapshot yet" with prominent refresh button
       - Uses shadcn Card with sections

    4. Rewrite `apps/web/app/(app)/dashboard/page.tsx` (replace current placeholder):
       - React Server Component
       - Data fetching:
         a. getAccounts(orgId) — for account summary cards
         b. getRecentTransactions(orgId, 6) — for cash flow chart data
         c. getLatestSnapshot(orgId) — for patrimony display
       - Compute cash flow: import aggregateCashFlow from @floow/core-finance, pass transactions
       - Layout sections (top to bottom):
         a. **Header**: "Dashboard Financeiro" title
         b. **Account Summary Row**: Grid of mini cards — one per account showing name, type icon, and balance. Plus a total balance card.
         c. **Quick Stats Row**: 3 stat cards — "Receitas do Mes" (current month income total), "Despesas do Mes" (current month expense total), "Saldo do Mes" (net). Use green/red/neutral colors.
         d. **Cash Flow Chart**: CashFlowChart component (from Plan 03) with last 6 months
         e. **Patrimony Snapshot**: PatrimonySummary component with refreshSnapshot action
       - Empty states: If no accounts, show "Create your first account" CTA linking to /accounts/new. If no transactions, show cash flow chart area with "Import or register transactions" message.
       - Uses shadcn Card, Button, lucide-react icons

    5. Create `apps/web/__tests__/finance/dashboard.test.tsx`:
       - Import { render, screen } from @testing-library/react
       - Mock the data-fetching queries and server actions (vi.mock)
       - Extract the dashboard's client sub-components (account cards, stats, PatrimonySummary) for unit testing since the dashboard itself is an RSC and cannot be rendered directly by RTL
       - Test account card rendering: pass Account[] props, verify each account name and formatted balance appears in the document
       - Test stats section: verify "Receitas do Mes", "Despesas do Mes", "Saldo do Mes" labels render
       - Test CashFlowChart renders with data-testid="cash-flow-chart" (add data-testid to CashFlowChart wrapper)
       - Test PatrimonySummary renders net worth value and "Update Snapshot" button
       - Test empty state: when no accounts passed, verify "Create your first account" CTA text appears
       - Follow existing test patterns from apps/web/__tests__/billing/webhook.test.ts

    6. Verify tests, typecheck, and build:
       ```
       pnpm --filter @floow/web test -- dashboard
       pnpm turbo run typecheck --filter=@floow/web
       pnpm turbo run build --filter=@floow/web
       ```
  </action>
  <verify>
    <automated>cd "C:/Users/rdeli/OneDrive/Área de Trabalho/floow" && pnpm --filter @floow/web test -- dashboard && pnpm --filter @floow/core-finance test && pnpm turbo run typecheck --filter=@floow/web && pnpm turbo run build --filter=@floow/web</automated>
  </verify>
  <done>Dashboard page shows account summary, quick monthly stats, cash flow chart, and patrimony snapshot. RTL tests verify account cards, stats labels, CashFlowChart, and PatrimonySummary render correctly. Empty states tested. Refresh snapshot button works. All tests pass, typecheck and build pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Finance Engine end-to-end</name>
  <action>
    Human verifies the complete Finance Engine: account management (create/list), transaction registration (income/expense/transfer with categories), OFX/CSV import with preview and deduplication, monthly cash flow chart, financial dashboard with account summary and patrimony snapshots.

    Verification steps:
    1. Start the app: `pnpm dev --filter=@floow/web`
    2. Log in and navigate to Dashboard — should show empty state prompts
    3. Go to Contas (accounts) — click "New Account", create a checking account named "Nubank"
    4. Create a second account: savings account named "Poupanca"
    5. Go to Transacoes (transactions) — click "New Transaction":
       - Create an income transaction: R$5.000,00 to Nubank, category "Salario"
       - Create an expense transaction: R$200,00 from Nubank, category "Alimentacao"
       - Create a transfer: R$1.000,00 from Nubank to Poupanca
    6. Verify /accounts shows correct balances: Nubank = R$3.800,00, Poupanca = R$1.000,00
    7. Verify /transactions shows all 4 entries (transfer creates 2 rows)
    8. Go to /transactions/import — upload a test OFX or CSV file if available (or skip)
    9. Return to Dashboard:
       - Account summary should show both accounts with balances
       - Quick stats should show current month income/expenses
       - Cash flow chart should show current month bars
       - Click "Update Snapshot" — patrimony should show net worth = R$4.800,00
    10. Run all tests: `pnpm test` — all should pass
  </action>
  <verify>Human confirms all flows work correctly</verify>
  <done>User approves: accounts, transactions, import, dashboard, and patrimony snapshot all functional</done>
  <resume-signal>Type "approved" if all flows work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `pnpm test` passes (all packages — db, core-finance, web including dashboard RTL tests)
- `pnpm turbo run build` passes (full monorepo build)
- Dashboard shows account summary, cash flow chart, patrimony snapshot
- computePatrimonySnapshot correctly separates liquid assets and liabilities
- All Phase 2 requirements addressed: FIN-01 through FIN-05, DASH-01, VAL-01
</verification>

<success_criteria>
- Dashboard displays consolidated financial overview at /dashboard
- Account summary shows all accounts with balances
- Cash flow chart renders monthly income vs expense bars
- Patrimony snapshot shows net worth, liquid assets, liabilities
- User can refresh patrimony snapshot from dashboard
- Dashboard RTL tests pass verifying component rendering
- Full test suite green, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-finance-engine/02-04-SUMMARY.md`
</output>
