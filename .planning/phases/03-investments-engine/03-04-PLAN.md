---
phase: 03-investments-engine
plan: 04
type: execute
wave: 4
depends_on: [03-02, 03-03]
files_modified: []
autonomous: false
requirements: [INV-01, INV-02, INV-03, INV-04, INV-05, INV-06, INV-07, DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "User can register assets and log portfolio events end-to-end"
    - "Buy events debit the linked account (INV-07 cash flow integration)"
    - "Position table shows correct average cost and PnL"
    - "User can view historical price entries per asset (INV-06 price history)"
    - "Investment dashboard shows allocation chart and net worth evolution"
    - "Income dashboard shows dividend history and monthly estimate"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for the complete investments engine. Verify all 10 requirements work end-to-end in the browser.

Purpose: Confirm that schema, computation engine, CRUD, cash flow integration, price history display, and all three dashboards function correctly before marking Phase 3 complete.

Output: Human approval or issue list for gap closure.
</objective>

<execution_context>
@C:/Users/rdeli/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rdeli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-investments-engine/03-01-SUMMARY.md
@.planning/phases/03-investments-engine/03-02-SUMMARY.md
@.planning/phases/03-investments-engine/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end verification of complete investments engine</name>
  <files></files>
  <action>
    Human verifies the complete Investments Engine: asset registry, portfolio event logging with cash flow integration, position calculation with PnL, price history display, three investment dashboards (allocation, net worth evolution, income).

    Start the dev server (`pnpm dev --filter @floow/web`) and test the following flow:

    **1. Navigation (basic)**
    - Verify "Investimentos" link appears in main nav bar
    - Click it — should land on /investments page

    **2. Asset Registration (INV-01)**
    - Navigate to /investments/new
    - Create an asset: Ticker="PETR4", Name="Petrobras PN", Class="Acoes BR"
    - Create another: Ticker="HGLG11", Name="CGHG Logistica", Class="FIIs"
    - Create another: Ticker="IVVB11", Name="iShares S&P 500", Class="ETFs"

    **3. Portfolio Events (INV-02, INV-07)**
    - On /investments/new, log a Buy event for PETR4: qty=100, price=R$28.50 (2850 cents), account=your brokerage account
    - Log a Buy event for HGLG11: qty=50, price=R$165.00 (16500 cents)
    - Log a Dividend event for HGLG11: totalCents=R$45.00 (4500 cents)
    - Verify: Navigate to /transactions — you should see corresponding cash flow entries (buy = expense, dividend = income)
    - Verify: Navigate to /accounts — brokerage account balance should reflect the debits/credits

    **4. Position Table (INV-03, INV-05)**
    - Navigate to /investments
    - Verify PETR4 row: qty=100, avg cost=R$28.50, PnL shown
    - Verify HGLG11 row: qty=50, avg cost=R$165.00, dividends=R$45.00
    - Verify totals row at bottom

    **5. Price Update and Price History (INV-06)**
    - Update PETR4 current price to R$32.00 (3200 cents) using the inline price update
    - Verify unrealized PnL updates: should show positive gain
    - Update PETR4 price again to R$33.50 (3350 cents) to create a second history entry
    - Expand the "Historico de Precos" panel for PETR4 in the position table
    - Verify the price history table shows both entries (R$32.00 and R$33.50) with correct dates
    - Verify "Nenhum preco registrado" appears for IVVB11 (no prices entered yet)

    **6. Investment Dashboard (DASH-02)**
    - Navigate to /investments/dashboard
    - Verify portfolio summary cards (total value, PnL, dividends)
    - Verify allocation pie chart shows segments for Acoes BR, FIIs, ETFs with correct colors

    **7. Net Worth Evolution (DASH-03)**
    - On /investments/dashboard, verify line chart appears
    - Click "Update Snapshot" on /dashboard (financial dashboard) to generate a new snapshot
    - Return to /investments/dashboard — chart should show the snapshot point

    **8. Income Dashboard (DASH-04)**
    - Navigate to /investments/income
    - Verify the HGLG11 dividend appears in the income chart
    - Verify "Renda Mensal Estimada" shows a value

    **9. Split Test (INV-02 edge case)**
    - Log a Split event for PETR4: splitRatio=2.0 (2-for-1)
    - Return to /investments — verify qty doubled to 200, avg cost halved to R$14.25

    **10. Sell Test (INV-05)**
    - Log a Sell event for PETR4: qty=50, price=R$32.00
    - Verify qty reduced to 150, realized PnL shown (profit from selling above avg cost)
    - Verify corresponding income transaction in /transactions

    Resume signal: Type "approved" if all checks pass, or describe any issues found.
  </action>
  <verify>Human approval of all 10 verification steps</verify>
  <done>All INV-01 through INV-07 and DASH-02, DASH-03, DASH-04 requirements verified working end-to-end, including INV-06 price history display. User approves the complete investments engine.</done>
</task>

</tasks>

<verification>
Human approval of all 10 verification steps above.
</verification>

<success_criteria>
- All INV-01 through INV-07 requirements verified working end-to-end
- INV-06 price history panel shows historical price entries per asset
- All DASH-02, DASH-03, DASH-04 dashboards render correctly with real data
- Cash flow integration (INV-07) creates correct transaction entries
- Split and sell edge cases compute correctly
- User approves the complete investments engine
</success_criteria>

<output>
After completion, create `.planning/phases/03-investments-engine/03-04-SUMMARY.md`
</output>
