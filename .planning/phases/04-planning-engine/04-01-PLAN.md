---
phase: 04-planning-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/planning.ts
  - packages/db/src/index.ts
  - packages/db/src/client.ts
  - packages/shared/src/schemas/planning.ts
  - packages/shared/src/index.ts
  - packages/core-finance/src/simulation.ts
  - packages/core-finance/src/withdrawal.ts
  - packages/core-finance/src/succession.ts
  - packages/core-finance/src/index.ts
  - packages/core-finance/src/__tests__/simulation.test.ts
  - packages/core-finance/src/__tests__/withdrawal.test.ts
  - packages/core-finance/src/__tests__/succession.test.ts
  - supabase/migrations/00005_planning.sql
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04
  - PLAN-05

must_haves:
  truths:
    - "simulateRetirementScenario() produces yearly portfolio projections for accumulation and withdrawal phases"
    - "Three scenarios (conservative, base, aggressive) produce different trajectory curves"
    - "calculateFI() returns the correct FI number and the year the portfolio crosses it"
    - "calculateFI() returns null when FI is unreachable within maxSearchYears"
    - "simulateWithdrawal() in fixed-amount mode depletes the portfolio and marks depletion year"
    - "simulateWithdrawal() in percentage mode sustains the portfolio indefinitely"
    - "calcItcmd() returns the correct rate for known Brazilian states"
    - "calcLiquidityGap() returns zero gap when liquid assets cover settlement costs"
    - "Heir percentage validation rejects totals != 100"
  artifacts:
    - path: "packages/db/src/schema/planning.ts"
      provides: "retirementPlans, withdrawalStrategies, successionPlans, heirs tables"
      contains: "pgTable"
    - path: "packages/core-finance/src/simulation.ts"
      provides: "simulateRetirementScenario, calculateFI pure functions"
      exports: ["simulateRetirementScenario", "calculateFI", "SCENARIO_PRESETS"]
    - path: "packages/core-finance/src/withdrawal.ts"
      provides: "simulateWithdrawal pure function"
      exports: ["simulateWithdrawal"]
    - path: "packages/core-finance/src/succession.ts"
      provides: "calcItcmd, calcLiquidityGap, validateHeirPercentages pure functions"
      exports: ["calcItcmd", "calcLiquidityGap", "ITCMD_RATES_BY_STATE", "validateHeirPercentages"]
    - path: "packages/shared/src/schemas/planning.ts"
      provides: "Zod validation schemas for planning forms"
      exports: ["retirementPlanSchema", "withdrawalStrategySchema", "successionPlanSchema", "heirSchema"]
    - path: "supabase/migrations/00005_planning.sql"
      provides: "Planning tables with RLS policies"
      contains: "CREATE TABLE"
  key_links:
    - from: "packages/db/src/index.ts"
      to: "packages/db/src/schema/planning.ts"
      via: "barrel re-export"
      pattern: "export.*planning"
    - from: "packages/db/src/client.ts"
      to: "packages/db/src/schema/planning.ts"
      via: "fullSchema spread"
      pattern: "planningSchema"
    - from: "packages/core-finance/src/index.ts"
      to: "simulation.ts, withdrawal.ts, succession.ts"
      via: "barrel re-export"
      pattern: "export.*simulation|withdrawal|succession"
---

<objective>
Create the complete data layer and pure computation engine for Phase 4 (Planning Engine) with TDD.

Purpose: All five PLAN requirements depend on these shared foundations -- DB schema for persisted plans, Zod validation schemas for forms, and pure computation functions (simulation, withdrawal, succession) that power the UI pages in Plans 02 and 03.

Output: 4 new DB tables with RLS, 3 pure function modules with full test coverage, Zod schemas for all planning forms, SQL migration.
</objective>

<execution_context>
@C:/Users/rdeli/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rdeli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-planning-engine/04-CONTEXT.md
@.planning/phases/04-planning-engine/04-RESEARCH.md

<interfaces>
<!-- Existing interfaces this plan depends on -->

From packages/db/src/schema/auth.ts:
```typescript
export const orgs = pgTable('orgs', { id: uuid('id').primaryKey().defaultRandom(), ... })
```

From packages/db/src/client.ts:
```typescript
export function createDb(connectionString: string): ReturnType<typeof drizzle>
export function getDb(): ReturnType<typeof createDb>
```

From packages/db/src/index.ts:
```typescript
export * from './schema/auth'
export * from './schema/billing'
export * from './schema/finance'
export * from './schema/investments'
export * from './client'
```

From packages/core-finance/src/income.ts:
```typescript
export function estimateMonthlyIncome(events: IncomeEvent[], months?: number): number
```

From packages/core-finance/src/index.ts:
```typescript
export * from './balance'
export * from './types'
export * from './import/ofx'
export * from './import/csv'
export * from './cash-flow'
export * from './snapshot'
export * from './portfolio'
export * from './income'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB schema, SQL migration, Zod schemas, and package wiring</name>
  <files>
    packages/db/src/schema/planning.ts,
    packages/db/src/index.ts,
    packages/db/src/client.ts,
    packages/shared/src/schemas/planning.ts,
    packages/shared/src/index.ts,
    supabase/migrations/00005_planning.sql
  </files>
  <behavior>
    - TypeScript compiles without errors after adding planning schema
    - All 4 tables (retirementPlans, withdrawalStrategies, successionPlans, heirs) exported from @floow/db
    - Zod schemas validate correct inputs and reject invalid ones
  </behavior>
  <action>
    1. Create `packages/db/src/schema/planning.ts` with 4 tables following the exact pattern from `investments.ts`:
       - `retirementPlans`: id (uuid pk), orgId (FK orgs, cascade, UNIQUE for upsert), currentAge (int), retirementAge (int), lifeExpectancy (int, default 85), monthlyContributionCents (int), desiredMonthlyIncomeCents (int), inflationRate (numeric 5,4, default '0.04'), conservativeReturnRate (numeric 5,4, nullable), baseReturnRate (numeric 5,4, nullable), aggressiveReturnRate (numeric 5,4, nullable), contributionGrowthRate (numeric 5,4, nullable), createdAt, updatedAt
       - `withdrawalStrategies`: id (uuid pk), orgId (FK orgs, cascade, UNIQUE for upsert), mode (text, 'fixed'|'percentage'), fixedMonthlyAmountCents (int, nullable), percentageRate (numeric 5,4, nullable), liquidationPreset (text, default 'income_preserving'), customLiquidationOrder (text, nullable -- JSON array), createdAt, updatedAt
       - `successionPlans`: id (uuid pk), orgId (FK orgs, cascade, UNIQUE for upsert), brazilianState (text, nullable), estimatedFuneralCostsCents (int, default 1500000), estimatedLegalFeesCents (int, default 500000), additionalLiabilitiesCents (int, default 0), createdAt, updatedAt
       - `heirs`: id (uuid pk), successionPlanId (FK successionPlans, cascade), orgId (FK orgs, cascade), name (text), relationship (text), percentageShare (numeric 5,2), createdAt
       - Export inferred types: RetirementPlan, NewRetirementPlan, WithdrawalStrategy, NewWithdrawalStrategy, SuccessionPlan, NewSuccessionPlan, Heir, NewHeir
       - Add `uniqueIndex` on orgId for retirementPlans, withdrawalStrategies, successionPlans (enables .onConflictDoUpdate for upsert pattern per Research pitfall 5)

    2. Update `packages/db/src/index.ts`: add `export * from './schema/planning'`

    3. Update `packages/db/src/client.ts`: import `* as planningSchema from './schema/planning'` and spread into fullSchema

    4. Create `supabase/migrations/00005_planning.sql`:
       - CREATE TABLE for all 4 tables matching the Drizzle schema
       - UNIQUE INDEX on org_id for retirementPlans, withdrawalStrategies, successionPlans
       - RLS ENABLE on all 4 tables
       - RLS policies using `org_id IN (SELECT public.get_user_org_ids())` pattern (same as 00002 and 00003)
       - Indexes on org_id for all tables, succession_plan_id for heirs

    5. Create `packages/shared/src/schemas/planning.ts` with Zod schemas:
       - `retirementPlanSchema`: currentAge (int min 18 max 100), retirementAge (int min 20 max 100), lifeExpectancy (int min 50 max 120, default 85), monthlyContributionCents (int min 0), desiredMonthlyIncomeCents (int min 0), inflationRate (number min 0 max 0.5, optional default 0.04), conservativeReturnRate/baseReturnRate/aggressiveReturnRate (number min 0 max 0.5, optional), contributionGrowthRate (number min 0 max 0.3, optional)
       - `withdrawalStrategySchema`: mode (enum 'fixed'|'percentage'), fixedMonthlyAmountCents (int, optional), percentageRate (number min 0.01 max 0.2, optional), liquidationPreset (string, default 'income_preserving'), customLiquidationOrder (string array, optional)
       - `successionPlanSchema`: brazilianState (string 2 chars, optional), estimatedFuneralCostsCents (int, optional default 1500000), estimatedLegalFeesCents (int, optional default 500000), additionalLiabilitiesCents (int, optional default 0)
       - `heirSchema`: name (string min 1 max 200), relationship (string min 1 max 100), percentageShare (number min 0 max 100)
       - Export inferred types: RetirementPlanInput, WithdrawalStrategyInput, SuccessionPlanInput, HeirInput

    6. Update `packages/shared/src/index.ts`: add `export * from './schemas/planning'`
  </action>
  <verify>
    <automated>cd "C:/Users/rdeli/OneDrive/Área de Trabalho/floow" && pnpm exec tsc --noEmit --project packages/db/tsconfig.json && pnpm exec tsc --noEmit --project packages/shared/tsconfig.json</automated>
  </verify>
  <done>
    All 4 planning tables defined in Drizzle with unique org_id indexes, exported from @floow/db. SQL migration with RLS policies ready. Zod schemas for all planning forms exported from @floow/shared. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Simulation and FI pure functions with TDD</name>
  <files>
    packages/core-finance/src/simulation.ts,
    packages/core-finance/src/__tests__/simulation.test.ts
  </files>
  <behavior>
    - simulateRetirementScenario({currentPortfolioCents: 50000000, monthlyContributionCents: 500000, currentAge: 30, retirementAge: 65, lifeExpectancy: 85, desiredMonthlyIncomeCents: 1000000, annualRealReturnRate: 0.06, annualContributionGrowthRate: 0.03}) returns array of 56 points (age 30..85), each with year/age/portfolioCents as integers
    - Accumulation phase (age < retirementAge): portfolio grows by return + contributions
    - Withdrawal phase (age >= retirementAge): portfolio grows by return - desired income; floor at 0
    - Three scenarios with different rates produce three different final portfolio values
    - calculateFI({currentPortfolioCents: 100000000, monthlyContributionCents: 500000, targetMonthlyPassiveIncomeCents: 1000000, annualRealReturnRate: 0.06, currentAge: 30}) returns fiNumberCents = 200000000 (12M/0.06), and a finite fiYear
    - calculateFI with very low portfolio and very high target returns null fiYear (unreachable)
    - SCENARIO_PRESETS exports three preset configs (conservative: 0.04/0.02, base: 0.06/0.03, aggressive: 0.09/0.04)
  </behavior>
  <action>
    Follow RED-GREEN-REFACTOR cycle.

    RED: Create `packages/core-finance/src/__tests__/simulation.test.ts` with the following test cases before writing any production code:
    1. `simulateRetirementScenario` returns correct number of data points (lifeExpectancy - currentAge + 1)
    2. First point has portfolioCents equal to currentPortfolioCents
    3. Portfolio grows during accumulation phase (age < retirementAge)
    4. Portfolio can deplete during withdrawal phase when income > returns
    5. Portfolio floor is 0 (never negative)
    6. All portfolioCents values are integers (Math.round applied)
    7. `calculateFI` returns correct fiNumberCents (targetAnnualIncome / safeWithdrawalRate)
    8. `calculateFI` returns finite fiYear when portfolio will reach FI number
    9. `calculateFI` returns null fiYear when FI is unreachable (tiny portfolio, huge target, 60 year limit)
    10. `SCENARIO_PRESETS` has conservative/base/aggressive with expected rates
    Run tests -- all should fail.

    GREEN: Create `packages/core-finance/src/simulation.ts` with:
    - `RetirementScenarioParams` interface: currentPortfolioCents, monthlyContributionCents, currentAge, retirementAge, lifeExpectancy, desiredMonthlyIncomeCents, annualRealReturnRate, annualContributionGrowthRate (all numbers)
    - `RetirementYearPoint` interface: year (number), age (number), portfolioCents (number)
    - `simulateRetirementScenario(params: RetirementScenarioParams): RetirementYearPoint[]` -- yearly loop from currentAge to lifeExpectancy; accumulation phase applies return + contributions; withdrawal phase applies return - desired income; Math.round at each step; floor at 0
    - `FIParams` interface: currentPortfolioCents, monthlyContributionCents, targetMonthlyPassiveIncomeCents, annualRealReturnRate, currentAge, maxSearchYears (optional, default 60)
    - `FIResult` interface: fiNumberCents, fiYear (number|null), yearsToFI (number|null)
    - `calculateFI(params: FIParams): FIResult` -- FI number = (target * 12) / rate; year-by-year loop checking if portfolio >= fiNumberCents; return null if not reached
    - `SCENARIO_PRESETS` const object with three entries:
      - conservative: { annualRealReturnRate: 0.04, annualContributionGrowthRate: 0.02 }
      - base: { annualRealReturnRate: 0.06, annualContributionGrowthRate: 0.03 }
      - aggressive: { annualRealReturnRate: 0.09, annualContributionGrowthRate: 0.04 }
    - Use integer cents throughout; Math.round() at every step per Research pitfall 2
    Run tests -- all should pass.

    REFACTOR if needed, ensure tests still pass.
  </action>
  <verify>
    <automated>cd "C:/Users/rdeli/OneDrive/Área de Trabalho/floow" && pnpm --filter @floow/core-finance test -- simulation</automated>
  </verify>
  <done>
    simulateRetirementScenario produces correct yearly projections with integer cents, floor at 0. calculateFI returns correct FI number and year. SCENARIO_PRESETS exported. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Withdrawal and succession pure functions with TDD, barrel export</name>
  <files>
    packages/core-finance/src/withdrawal.ts,
    packages/core-finance/src/succession.ts,
    packages/core-finance/src/__tests__/withdrawal.test.ts,
    packages/core-finance/src/__tests__/succession.test.ts,
    packages/core-finance/src/index.ts
  </files>
  <behavior>
    - simulateWithdrawal fixed mode: portfolio of 120000000 (R$1.2M) with R$10k/mo withdrawal at 4% return depletes eventually; points include depleted=true and portfolioCents=0 after depletion
    - simulateWithdrawal percentage mode (4%): portfolio never fully depletes (sustainable withdrawal)
    - calcItcmd('SP', 100000000) returns 4000000 (4% of R$1M)
    - calcItcmd('BA', 100000000) returns 8000000 (8% of R$1M)
    - calcItcmd with unknown state falls back to 5%
    - calcLiquidityGap returns 0 gap when liquid assets >= required liquidity
    - calcLiquidityGap returns positive gap when liquid assets < required liquidity
    - validateHeirPercentages([{share: 50}, {share: 50}]) returns true
    - validateHeirPercentages([{share: 60}, {share: 50}]) returns false
    - validateHeirPercentages([]) returns false (empty heir list)
  </behavior>
  <action>
    Follow RED-GREEN-REFACTOR cycle for BOTH modules.

    **Withdrawal module:**

    RED: Create `packages/core-finance/src/__tests__/withdrawal.test.ts`:
    1. Fixed mode: returns correct number of points (endAge - startAge + 1)
    2. Fixed mode: portfolio depletes when withdrawal > returns; depleted flag set
    3. Fixed mode: portfolioCents floor at 0 after depletion
    4. Fixed mode: withdrawalCents is 0 after depletion
    5. Percentage mode (4%): portfolio remains positive (sustainable)
    6. All values are integers (Math.round)

    GREEN: Create `packages/core-finance/src/withdrawal.ts`:
    - `WithdrawalParams` interface: initialPortfolioCents, mode ('fixed'|'percentage'), fixedMonthlyWithdrawalCents (optional), percentageRate (optional, default 0.04), annualRealReturnRate, startAge, endAge
    - `WithdrawalYearPoint` interface: year, age, portfolioCents, withdrawalCents, depleted (boolean)
    - `simulateWithdrawal(params: WithdrawalParams): WithdrawalYearPoint[]` -- yearly loop from startAge to endAge; fixed mode: withdrawal = fixedMonthly * 12; percentage mode: withdrawal = portfolio * rate; apply return then subtract withdrawal; floor at 0; mark depleted
    - Follow the exact code pattern from 04-RESEARCH.md with Math.round at each step

    **Succession module:**

    RED: Create `packages/core-finance/src/__tests__/succession.test.ts`:
    1. calcItcmd returns correct rate for SP (4%)
    2. calcItcmd returns correct rate for BA (8%)
    3. calcItcmd falls back to 5% for unknown state
    4. calcLiquidityGap returns 0 when liquid assets cover all costs
    5. calcLiquidityGap returns positive gap when liquid assets insufficient
    6. calcLiquidityGap includes ITCMD + funeral + legal + additional in required liquidity
    7. validateHeirPercentages returns true when shares sum to exactly 100
    8. validateHeirPercentages returns false when shares sum to != 100
    9. validateHeirPercentages returns false for empty array

    GREEN: Create `packages/core-finance/src/succession.ts`:
    - `ITCMD_RATES_BY_STATE` const Record<string, number> with all 27 Brazilian states (AC..TO) from Research
    - `calcItcmd(estateCents: number, state: string): number` -- lookup rate, fallback 0.05, Math.round
    - `LiquidityGapParams` interface: totalEstateCents, liquidAssetsCents, brazilianState, estimatedFuneralCostsCents (optional default 1500000), estimatedLegalFeesCents (optional default 500000), additionalLiabilitiesCents (optional default 0)
    - `LiquidityGapResult` interface: requiredLiquidityCents, liquidityGapCents, itcmdTotalCents
    - `calcLiquidityGap(params: LiquidityGapParams): LiquidityGapResult`
    - `validateHeirPercentages(shares: number[]): boolean` -- returns true only if shares.length > 0 and sum === 100 (use Math.round to handle floating point)

    **Barrel export:**
    Update `packages/core-finance/src/index.ts`: add exports for simulation, withdrawal, succession modules.

    REFACTOR if needed, run all tests.
  </action>
  <verify>
    <automated>cd "C:/Users/rdeli/OneDrive/Área de Trabalho/floow" && pnpm --filter @floow/core-finance test</automated>
  </verify>
  <done>
    simulateWithdrawal handles fixed and percentage modes correctly with depletion tracking. calcItcmd returns correct rates for all states. calcLiquidityGap computes correct gap. validateHeirPercentages enforces sum = 100. All existing + new tests pass. Barrel index updated.
  </done>
</task>

</tasks>

<verification>
- All 3 pure function modules compile and export correctly from @floow/core-finance
- All planning tables defined in Drizzle and exported from @floow/db
- SQL migration ready for deployment with RLS policies
- Zod schemas exported from @floow/shared
- Full test suite passes: `pnpm --filter @floow/core-finance test`
- TypeScript compiles: `pnpm exec tsc --noEmit --project packages/core-finance/tsconfig.json`
</verification>

<success_criteria>
- 4 DB tables (retirementPlans, withdrawalStrategies, successionPlans, heirs) with unique org_id constraints and RLS
- 3 pure function modules (simulation.ts, withdrawal.ts, succession.ts) with full TDD coverage
- Zod validation schemas for all 5 planning form types
- All tests green, TypeScript compiles cleanly
- No external packages needed (everything already installed)
</success_criteria>

<output>
After completion, create `.planning/phases/04-planning-engine/04-01-SUMMARY.md`
</output>
