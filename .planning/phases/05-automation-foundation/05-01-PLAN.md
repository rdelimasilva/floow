---
phase: 05-automation-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00006_automation.sql
  - packages/core-finance/package.json
  - packages/core-finance/src/categorization.ts
  - packages/core-finance/src/recurring.ts
  - packages/core-finance/src/__tests__/categorization.test.ts
  - packages/core-finance/src/__tests__/recurring.test.ts
  - packages/core-finance/src/index.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Migration 00006_automation.sql creates category_rules and recurring_templates tables with all columns, constraints, and RLS policies"
    - "transactions table gains a nullable recurring_template_id column with FK to recurring_templates"
    - "Unique partial index on (recurring_template_id, date) prevents duplicate generation"
    - "matchCategory returns the correct category ID for matching rules by priority order"
    - "matchCategory returns null when no rule matches or input is empty"
    - "advanceByFrequency correctly advances dates for all 6 frequencies including month-end clamp"
    - "getOverdueDates returns all due dates up to reference date and empty array for future dates"
    - "@floow/core-finance builds and exports all new functions without breaking existing exports"
  artifacts:
    - path: "supabase/migrations/00006_automation.sql"
      provides: "category_rules and recurring_templates tables, ALTER on transactions, RLS, indexes"
      contains: "CREATE TABLE public.category_rules"
    - path: "packages/core-finance/src/categorization.ts"
      provides: "matchCategory pure function and CategoryRule interface"
      exports: ["matchCategory", "CategoryRule", "MatchType"]
    - path: "packages/core-finance/src/recurring.ts"
      provides: "advanceByFrequency and getOverdueDates pure functions"
      exports: ["advanceByFrequency", "getOverdueDates", "RecurringFrequency"]
    - path: "packages/core-finance/src/__tests__/categorization.test.ts"
      provides: "Unit tests for matchCategory"
      min_lines: 40
    - path: "packages/core-finance/src/__tests__/recurring.test.ts"
      provides: "Unit tests for advanceByFrequency and getOverdueDates"
      min_lines: 60
    - path: "packages/core-finance/src/index.ts"
      provides: "Barrel exports including new modules"
      contains: "export * from './categorization'"
  key_links:
    - from: "packages/core-finance/src/index.ts"
      to: "packages/core-finance/src/categorization.ts"
      via: "barrel re-export"
      pattern: "export \\* from './categorization'"
    - from: "packages/core-finance/src/index.ts"
      to: "packages/core-finance/src/recurring.ts"
      via: "barrel re-export"
      pattern: "export \\* from './recurring'"
    - from: "packages/core-finance/src/recurring.ts"
      to: "date-fns"
      via: "named import"
      pattern: "import.*from 'date-fns'"
---

<objective>
Create the database schema migration and pure TypeScript functions for both v1.1 automation features: category rules and recurring transactions.

Purpose: Phases 6 and 7 build directly on this foundation. The migration provides the tables, constraints, and RLS policies. The pure functions provide tested business logic that server actions will call.

Output: One SQL migration file, two TypeScript modules with unit tests, updated barrel exports, date-fns dependency added.
</objective>

<execution_context>
@C:/Users/rdeli/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rdeli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-automation-foundation/05-CONTEXT.md
@.planning/phases/05-automation-foundation/05-RESEARCH.md

<interfaces>
<!-- Existing codebase contracts the executor needs -->

From supabase/migrations/00002_finance.sql (transactions table being ALTERed):
```sql
CREATE TABLE public.transactions (
  id                uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  org_id            uuid NOT NULL REFERENCES public.orgs(id) ON DELETE CASCADE,
  account_id        uuid NOT NULL REFERENCES public.accounts(id) ON DELETE CASCADE,
  category_id       uuid REFERENCES public.categories(id) ON DELETE SET NULL,
  type              transaction_type NOT NULL,
  amount_cents      integer NOT NULL,
  description       text NOT NULL,
  date              date NOT NULL,
  transfer_group_id uuid,
  imported_at       timestamptz,
  external_id       text,
  created_at        timestamptz NOT NULL DEFAULT now()
);
```

From packages/core-finance/src/index.ts (barrel to extend):
```typescript
// Core finance engine -- Phase 2
export * from './balance'
export * from './types'
export * from './import/ofx'
export * from './import/csv'
export * from './cash-flow'
export * from './snapshot'
// Phase 3 -- Investment engine
export * from './portfolio'
export * from './income'
// Phase 4 -- Planning engine
export * from './simulation'
export * from './withdrawal'
export * from './succession'
```

From packages/core-finance/package.json (to add date-fns):
```json
{
  "dependencies": {
    "@floow/db": "workspace:*",
    "drizzle-orm": "^0.40.0",
    "ofx-js": "^0.2.0",
    "papaparse": "^5.5.3",
    "zod": "catalog:"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 00006_automation.sql and add date-fns dependency</name>
  <files>supabase/migrations/00006_automation.sql, packages/core-finance/package.json</files>
  <action>
**Migration file** (`supabase/migrations/00006_automation.sql`):

Follow the exact structural pattern from 00005_planning.sql: banner comment, TABLES section, INDEXES section, RLS section.

1. Enable pg_trgm extension: `CREATE EXTENSION IF NOT EXISTS pg_trgm;` at the top (idempotent guard).

2. Create `category_rules` table:
   - `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`
   - `org_id uuid NOT NULL REFERENCES public.orgs(id) ON DELETE CASCADE`
   - `category_id uuid NOT NULL REFERENCES public.categories(id) ON DELETE CASCADE`
   - `match_type text NOT NULL CHECK (match_type IN ('contains', 'exact'))`
   - `match_value text NOT NULL`
   - `priority integer NOT NULL DEFAULT 0`
   - `is_enabled boolean NOT NULL DEFAULT true`
   - `created_at timestamptz NOT NULL DEFAULT now()`
   - `updated_at timestamptz NOT NULL DEFAULT now()`

3. Create `recurring_templates` table:
   - `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`
   - `org_id uuid NOT NULL REFERENCES public.orgs(id) ON DELETE CASCADE`
   - `account_id uuid NOT NULL REFERENCES public.accounts(id) ON DELETE CASCADE`
   - `category_id uuid REFERENCES public.categories(id) ON DELETE SET NULL` (nullable -- template can exist without category)
   - `type transaction_type NOT NULL`
   - `amount_cents integer NOT NULL`
   - `description text NOT NULL`
   - `frequency text NOT NULL CHECK (frequency IN ('daily','weekly','biweekly','monthly','quarterly','yearly'))`
   - `next_due_date date NOT NULL`
   - `is_active boolean NOT NULL DEFAULT true`
   - `notes text` (optional)
   - `created_at timestamptz NOT NULL DEFAULT now()`
   - `updated_at timestamptz NOT NULL DEFAULT now()`

4. ALTER `transactions` table: add `recurring_template_id uuid REFERENCES public.recurring_templates(id) ON DELETE SET NULL` column. This must come AFTER recurring_templates CREATE TABLE.

5. INDEXES section:
   - `CREATE UNIQUE INDEX uq_generated_transactions ON public.transactions (recurring_template_id, date) WHERE recurring_template_id IS NOT NULL;` -- partial unique index for deduplication (uses `date` column, not `due_date`, per research recommendation)
   - `CREATE INDEX idx_category_rules_org_id ON public.category_rules USING btree (org_id);` -- RLS performance
   - `CREATE INDEX idx_category_rules_match_value ON public.category_rules USING gin (match_value gin_trgm_ops);` -- enables ILIKE queries in Phase 6
   - `CREATE INDEX idx_recurring_templates_org_id ON public.recurring_templates USING btree (org_id);` -- RLS performance
   - `CREATE INDEX idx_recurring_templates_next_due_date ON public.recurring_templates USING btree (next_due_date);` -- upcoming-due queries in Phase 7

6. RLS section: Enable RLS on both `category_rules` and `recurring_templates`. Create 4 policies each (select/insert/update/delete) following the exact naming and structure from 00005_planning.sql:
   - Policy name format: `"table_name: members can {verb}"`
   - All use `TO authenticated` and `USING (org_id IN (SELECT public.get_user_org_ids()))`
   - INSERT uses `WITH CHECK` instead of `USING`

**date-fns dependency**: Run `pnpm add date-fns@^4.1.0` inside `packages/core-finance/`. Do NOT modify package.json manually -- use pnpm add to ensure the lockfile is updated.
  </action>
  <verify>
    <automated>cd "C:/Users/rdeli/OneDrive/Área de Trabalho/floow" && cat supabase/migrations/00006_automation.sql | head -5 && grep -c "CREATE TABLE" supabase/migrations/00006_automation.sql && grep -c "CREATE POLICY" supabase/migrations/00006_automation.sql && grep "date-fns" packages/core-finance/package.json</automated>
  </verify>
  <done>
    - 00006_automation.sql exists with 2 CREATE TABLE statements, 1 ALTER TABLE, 5+ indexes, 8 RLS policies
    - pg_trgm extension enabled at top of migration
    - recurring_template_id column added to transactions via ALTER TABLE
    - Unique partial index on (recurring_template_id, date) WHERE recurring_template_id IS NOT NULL
    - date-fns@^4.1.0 present in packages/core-finance/package.json dependencies
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create categorization and recurring pure functions with TDD tests and wire barrel exports</name>
  <files>
    packages/core-finance/src/categorization.ts,
    packages/core-finance/src/recurring.ts,
    packages/core-finance/src/__tests__/categorization.test.ts,
    packages/core-finance/src/__tests__/recurring.test.ts,
    packages/core-finance/src/index.ts
  </files>
  <behavior>
    matchCategory:
    - Returns null when rules array is empty
    - Returns null when description is empty string
    - Returns categoryId for exact match (case-insensitive: "Netflix" matches "netflix")
    - Returns categoryId for contains match (case-insensitive: "Pagamento Netflix" matches rule with matchValue "netflix")
    - Returns null when no rule matches
    - Returns highest-priority match when multiple rules match (rules pre-sorted by priority DESC)
    - Skips disabled rules (isEnabled: false) -- function iterates all passed rules, caller is responsible for filtering but if a disabled rule is passed it should still be evaluated (document precondition)

    advanceByFrequency:
    - daily: adds 1 day
    - weekly: adds 7 days
    - biweekly: adds 14 days
    - monthly: adds 1 month (Jan 31 -> Feb 28 clamp)
    - quarterly: adds 3 months
    - yearly: adds 1 year
    - monthly month-end chain: Jan 31 -> Feb 28 -> Mar 28 (clamp sticks, does not revert to 31)

    getOverdueDates:
    - Returns empty array when nextDueDate is after referenceDate
    - Returns single date when nextDueDate equals referenceDate
    - Returns multiple dates for overdue monthly template (Jan 1 with Mar 18 ref -> [Jan 1, Feb 1, Mar 1])
    - Returns correct dates for daily frequency across DST boundary
    - Returns correct dates for yearly frequency spanning leap year
  </behavior>
  <action>
**TDD flow: Write tests FIRST, then implement, then wire.**

**Step 1: Create test files (RED phase)**

Create `packages/core-finance/src/__tests__/categorization.test.ts`:
- Import from `../categorization` (not index barrel)
- Import `CategoryRule` type
- One `describe('matchCategory', ...)` block
- Test cases per the behavior list above
- Use explicit rule arrays with `id`, `matchType`, `matchValue`, `categoryId`, `priority`, `isEnabled` fields

Create `packages/core-finance/src/__tests__/recurring.test.ts`:
- Import from `../recurring`
- Import `{ addDays, addWeeks, addMonths, addQuarters, addYears } from 'date-fns'` only for expected value computation in tests if needed (or use hardcoded ISO dates)
- `describe('advanceByFrequency', ...)` with all 6 frequencies + month-end edge cases
- `describe('getOverdueDates', ...)` with empty/single/multiple date scenarios
- Use `new Date('YYYY-MM-DD')` for deterministic dates (no `new Date()`)
- Compare results with `.toISOString().startsWith('YYYY-MM-DD')` pattern (avoids timezone issues in test assertions)

Run tests -- they MUST fail (modules don't exist yet).

**Step 2: Create implementation (GREEN phase)**

Create `packages/core-finance/src/categorization.ts`:
- Export `MatchType = 'contains' | 'exact'`
- Export `CategoryRule` interface with fields: `id: string`, `matchType: MatchType`, `matchValue: string`, `categoryId: string`, `priority: number`, `isEnabled: boolean`
- Export `matchCategory(description: string, rules: CategoryRule[]): string | null`
- Case-insensitive comparison using `.toLowerCase()` on both description and matchValue
- For 'exact': `lowerDesc === lowerValue`
- For 'contains': `lowerDesc.includes(lowerValue)`
- Return first match (rules assumed pre-sorted by priority DESC)
- Return null if description is falsy or rules is empty
- JSDoc documenting preconditions: rules must be pre-sorted by priority DESC, only enabled rules should be passed, caller must check `category_id IS NULL` before calling

Create `packages/core-finance/src/recurring.ts`:
- Import `{ addDays, addWeeks, addMonths, addQuarters, addYears }` from `'date-fns'` (named imports, v4 style)
- Export `RecurringFrequency` type union: `'daily' | 'weekly' | 'biweekly' | 'monthly' | 'quarterly' | 'yearly'`
- Export `advanceByFrequency(date: Date, frequency: RecurringFrequency): Date` -- switch on frequency, biweekly uses `addWeeks(date, 2)`
- Export `getOverdueDates(nextDueDate: Date, frequency: RecurringFrequency, referenceDate: Date): Date[]` -- loop from nextDueDate, push while `current <= referenceDate`, advance with `advanceByFrequency`
- JSDoc on all exports including month-end clamp behavior documentation
- No `new Date()` inside functions -- all dates passed as parameters (deterministic)

Run tests -- they MUST pass.

**Step 3: Wire barrel exports**

Append to `packages/core-finance/src/index.ts`:
```typescript
// Phase 5 -- Automation foundation
export * from './categorization'
export * from './recurring'
```

Run full test suite and typecheck to verify no regressions.
  </action>
  <verify>
    <automated>cd "C:/Users/rdeli/OneDrive/Área de Trabalho/floow" && pnpm --filter @floow/core-finance test && pnpm --filter @floow/core-finance typecheck</automated>
  </verify>
  <done>
    - categorization.test.ts has 6+ test cases, all passing
    - recurring.test.ts has 8+ test cases covering all 6 frequencies and edge cases, all passing
    - matchCategory returns correct categoryId or null for all match types
    - advanceByFrequency handles month-end clamp correctly (Jan 31 -> Feb 28 -> Mar 28)
    - getOverdueDates returns correct array of dates up to reference date
    - index.ts re-exports both new modules
    - Full test suite passes (existing 106 tests + new tests)
    - Typecheck passes with no errors
  </done>
</task>

</tasks>

<verification>
1. Migration syntax: `cat supabase/migrations/00006_automation.sql` -- visually verify structure matches 00005 pattern
2. Test suite: `pnpm --filter @floow/core-finance test` -- all tests pass (106 existing + new)
3. Type safety: `pnpm --filter @floow/core-finance typecheck` -- no TypeScript errors
4. Exports: `grep "export \* from" packages/core-finance/src/index.ts` -- categorization and recurring present
5. date-fns: `grep "date-fns" packages/core-finance/package.json` -- ^4.1.0 in dependencies
</verification>

<success_criteria>
- 00006_automation.sql creates both tables with all columns, constraints, RLS policies, and indexes
- transactions table has recurring_template_id column with partial unique index
- matchCategory pure function tested with exact/contains/priority/case-insensitivity/null cases
- advanceByFrequency tested for all 6 frequencies including month-end clamp chain
- getOverdueDates tested for future/equal/overdue scenarios
- @floow/core-finance builds, exports new symbols, and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-automation-foundation/05-01-SUMMARY.md`
</output>
