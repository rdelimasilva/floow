---
phase: 02-finance-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/finance.ts
  - packages/db/src/index.ts
  - packages/db/src/client.ts
  - supabase/migrations/00002_finance.sql
  - packages/core-finance/package.json
  - packages/core-finance/tsconfig.json
  - packages/core-finance/src/index.ts
  - packages/core-finance/src/balance.ts
  - packages/core-finance/src/types.ts
  - packages/shared/src/schemas/finance.ts
  - packages/shared/src/index.ts
  - packages/db/src/__tests__/finance-schema.test.ts
  - packages/core-finance/src/__tests__/balance.test.ts
  - packages/core-finance/vitest.config.ts
autonomous: true
requirements: [FIN-01, FIN-02, FIN-03]

must_haves:
  truths:
    - "Schema tests pass verifying all 4 tables export correct columns, enums, and inferred types"
    - "Migration creates 4 tables (accounts, transactions, categories, patrimony_snapshots) with RLS enabled on each"
    - "Balance utility tests pass for centsToCurrency, currencyToCents, and formatBRL"
    - "Unique constraint on (externalId, accountId) in transactions table supports ON CONFLICT deduplication"
    - "11 system categories are seeded in the migration with org_id = NULL and is_system = true"
    - "Drizzle client includes finance schema in createDb factory"
  artifacts:
    - path: "packages/db/src/schema/finance.ts"
      provides: "accounts, transactions, categories, patrimonySnapshots tables + enums + inferred types"
      exports: ["accounts", "transactions", "categories", "patrimonySnapshots", "accountTypeEnum", "transactionTypeEnum", "Account", "NewAccount", "Transaction", "NewTransaction", "Category", "PatrimonySnapshot"]
    - path: "supabase/migrations/00002_finance.sql"
      provides: "SQL tables, indexes (including UNIQUE on external_id+account_id), RLS policies, system category seed"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "packages/core-finance/src/balance.ts"
      provides: "centsToCurrency, currencyToCents, formatBRL helper functions"
      exports: ["centsToCurrency", "currencyToCents", "formatBRL"]
    - path: "packages/shared/src/schemas/finance.ts"
      provides: "Zod schemas for account and transaction form validation"
      exports: ["createAccountSchema", "createTransactionSchema"]
  key_links:
    - from: "packages/db/src/client.ts"
      to: "packages/db/src/schema/finance.ts"
      via: "import * as financeSchema; spread into fullSchema"
      pattern: "financeSchema"
    - from: "packages/db/src/index.ts"
      to: "packages/db/src/schema/finance.ts"
      via: "barrel re-export"
      pattern: "export \\* from './schema/finance'"
---

<objective>
Create the finance data layer: Drizzle schema for accounts, transactions, categories, and patrimony snapshots; SQL migration with RLS policies and system category seed; core-finance package bootstrap with balance utilities and test infrastructure.

Purpose: Every subsequent plan depends on this schema and core-finance foundation. Without it, no UI, import, or dashboard work can proceed.
Output: Schema types exported from @floow/db, RLS migration ready for Supabase, core-finance package with vitest and balance helpers, Zod validation schemas in @floow/shared.
</objective>

<execution_context>
@C:/Users/rdeli/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rdeli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-finance-engine/02-RESEARCH.md

@packages/db/src/schema/auth.ts
@packages/db/src/schema/billing.ts
@packages/db/src/client.ts
@packages/db/src/index.ts
@packages/core-finance/package.json
@packages/core-finance/src/index.ts
@packages/shared/src/schemas/auth.ts
@packages/shared/src/index.ts

<interfaces>
<!-- Existing codebase contracts this plan extends -->

From packages/db/src/schema/auth.ts:
```typescript
export const orgs = pgTable('orgs', {
  id: uuid('id').primaryKey().defaultRandom(),
  name: text('name').notNull(),
  type: orgTypeEnum('type').notNull().default('personal'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
})
export type Org = typeof orgs.$inferSelect
```

From packages/db/src/client.ts:
```typescript
import * as schema from './schema/auth'
import * as billingSchema from './schema/billing'
const fullSchema = { ...schema, ...billingSchema }
export function createDb(connectionString: string) { ... }
export const db = createDb(process.env.DATABASE_URL ?? '')
```

From packages/db/src/index.ts:
```typescript
export * from './schema/auth'
export * from './schema/billing'
export * from './client'
```

From packages/shared/src/index.ts:
```typescript
export * from './schemas/auth'
export * from './schemas/billing'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Drizzle finance schema, Zod validation schemas, and Wave 0 tests</name>
  <files>
    packages/db/src/schema/finance.ts,
    packages/db/src/index.ts,
    packages/db/src/client.ts,
    packages/shared/src/schemas/finance.ts,
    packages/shared/src/index.ts,
    packages/db/src/__tests__/finance-schema.test.ts
  </files>
  <behavior>
    - Test: finance.ts exports accountTypeEnum with values ['checking', 'savings', 'brokerage', 'credit_card', 'cash']
    - Test: finance.ts exports transactionTypeEnum with values ['income', 'expense', 'transfer']
    - Test: accounts table has columns: id, orgId, name, type, balanceCents (integer), currency, isActive, createdAt, updatedAt
    - Test: transactions table has columns: id, orgId, accountId, categoryId, type, amountCents (integer), description, date, transferGroupId, importedAt, externalId, createdAt
    - Test: categories table has columns: id, orgId, name, type, color, icon, isSystem, createdAt
    - Test: patrimonySnapshots table has columns: id, orgId, snapshotDate, netWorthCents, liquidAssetsCents, liabilitiesCents, breakdown, createdAt
    - Test: All inferred types (Account, NewAccount, Transaction, NewTransaction, Category, PatrimonySnapshot) are exported
  </behavior>
  <action>
    1. Create `packages/db/src/__tests__/finance-schema.test.ts` with tests verifying schema exports, column names, and type exports BEFORE creating the schema. Run tests (RED).

    2. Create `packages/db/src/schema/finance.ts` following the exact pattern from 02-RESEARCH.md Pattern 1:
       - Import pgTable, pgEnum, uuid, text, integer, timestamp, boolean, index, uniqueIndex from drizzle-orm/pg-core
       - Import unique from drizzle-orm/pg-core (for composite unique constraints, following auth.ts pattern)
       - Import { orgs } from './auth' for FK reference
       - accountTypeEnum: ['checking', 'savings', 'brokerage', 'credit_card', 'cash']
       - transactionTypeEnum: ['income', 'expense', 'transfer']
       - accounts table with orgId FK to orgs, balanceCents as integer (default 0), currency default 'BRL', isActive default true, idx_accounts_org_id index
       - categories table with nullable orgId (null = system default), name, type (reuse transactionTypeEnum), color, icon, isSystem default false, idx_categories_org_id index
       - transactions table with orgId FK, accountId FK to accounts, categoryId FK to categories (onDelete set null), type, amountCents integer, description, date, transferGroupId, importedAt, externalId, idx on orgId + accountId + date. CRITICAL: Add a uniqueIndex('uq_transactions_external_account') on (externalId, accountId) in the table's third argument — this is REQUIRED for Plan 02-03's onConflictDoNothing deduplication to work at the PostgreSQL level. Use Drizzle's uniqueIndex() (not regular index()). Only non-null externalId rows will be affected since PostgreSQL treats NULLs as distinct in unique indexes.
       - patrimonySnapshots table with orgId FK, snapshotDate, netWorthCents, liquidAssetsCents, liabilitiesCents (default 0), breakdown (text for JSON), idx on orgId + date
       - Export inferred types: Account, NewAccount, Transaction, NewTransaction, Category, NewCategory, PatrimonySnapshot, NewPatrimonySnapshot

    3. Update `packages/db/src/index.ts` to add: `export * from './schema/finance'`

    4. Update `packages/db/src/client.ts` to import financeSchema and spread into fullSchema:
       ```
       import * as financeSchema from './schema/finance'
       const fullSchema = { ...schema, ...billingSchema, ...financeSchema }
       ```

    5. Create `packages/shared/src/schemas/finance.ts` with Zod schemas:
       - createAccountSchema: { name: z.string().min(1).max(100), type: z.enum(['checking', 'savings', 'brokerage', 'credit_card', 'cash']) }
       - createTransactionSchema: { accountId: z.string().uuid(), categoryId: z.string().uuid().optional(), type: z.enum(['income', 'expense', 'transfer']), amountCents: z.number().int().positive(), description: z.string().min(1).max(500), date: z.string().datetime() or z.coerce.date(), transferToAccountId: z.string().uuid().optional() }
       - Export inferred types: CreateAccountInput, CreateTransactionInput

    6. Update `packages/shared/src/index.ts` to add: `export * from './schemas/finance'`

    7. Run tests (GREEN). Verify: `pnpm --filter @floow/db test`
  </action>
  <verify>
    <automated>cd "C:/Users/rdeli/OneDrive/Área de Trabalho/floow" && pnpm --filter @floow/db test && pnpm turbo run typecheck</automated>
  </verify>
  <done>Drizzle finance schema exports all 4 tables + enums + types. Transactions table has uniqueIndex on (externalId, accountId) for import dedup. client.ts includes financeSchema. Zod validation schemas in @floow/shared. All schema tests pass. Typecheck passes across monorepo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SQL migration with RLS, indexes, system category seed, and core-finance bootstrap</name>
  <files>
    supabase/migrations/00002_finance.sql,
    packages/core-finance/package.json,
    packages/core-finance/tsconfig.json,
    packages/core-finance/vitest.config.ts,
    packages/core-finance/src/index.ts,
    packages/core-finance/src/types.ts,
    packages/core-finance/src/balance.ts,
    packages/core-finance/src/__tests__/balance.test.ts
  </files>
  <behavior>
    - Test: centsToCurrency(15075) returns formatted BRL string containing "150,75"
    - Test: centsToCurrency(0) returns formatted string containing "0,00"
    - Test: centsToCurrency(-5000) returns formatted negative BRL string
    - Test: currencyToCents(150.75) returns 15075
    - Test: currencyToCents("150,75") returns 15075
    - Test: currencyToCents(0) returns 0
    - Test: formatBRL(15075) returns "R$ 150,75" formatted string
  </behavior>
  <action>
    1. Bootstrap core-finance test infrastructure:
       - Update `packages/core-finance/package.json`: add vitest ^3.0.0 to devDependencies, add "test": "vitest run" script, add @floow/db as workspace:* dependency, add drizzle-orm ^0.40.0 as dependency
       - Create `packages/core-finance/vitest.config.ts` with standard config matching packages/db pattern
       - Run `pnpm install` from monorepo root to wire dependencies

    2. Create `packages/core-finance/src/types.ts` with shared types:
       - NormalizedTransaction interface: { externalId: string, date: Date, amountCents: number, description: string, type: 'income' | 'expense' }
       - CashFlowMonth interface: { month: string, income: number, expense: number, net: number }
       - Re-export Account, Transaction types from @floow/db for convenience

    3. Create `packages/core-finance/src/__tests__/balance.test.ts` with tests for centsToCurrency, currencyToCents, formatBRL BEFORE implementation. Run (RED).

    4. Create `packages/core-finance/src/balance.ts`:
       - centsToCurrency(cents: number): string — uses Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }) with cents/100
       - currencyToCents(value: string | number): number — handles both "150,75" and 150.75 formats, uses Math.round(parseFloat(normalized) * 100)
       - formatBRL(cents: number): string — alias for centsToCurrency for simpler imports

    5. Run tests (GREEN).

    6. Update `packages/core-finance/src/index.ts` to export from balance.ts and types.ts:
       ```
       export * from './balance'
       export * from './types'
       ```

    7. Create `supabase/migrations/00002_finance.sql`:
       - CREATE TYPE account_type AS ENUM ('checking', 'savings', 'brokerage', 'credit_card', 'cash');
       - CREATE TYPE transaction_type AS ENUM ('income', 'expense', 'transfer');
       - CREATE TABLE accounts (matching Drizzle schema exactly)
       - CREATE TABLE categories (matching Drizzle schema exactly)
       - CREATE TABLE transactions (matching Drizzle schema exactly)
       - CREATE TABLE patrimony_snapshots (matching Drizzle schema exactly)
       - All indexes matching Drizzle schema: idx_accounts_org_id, idx_categories_org_id, idx_transactions_org_id, idx_transactions_account_id, idx_transactions_date, idx_patrimony_snapshots_org_id, idx_patrimony_snapshots_date
       - CRITICAL: Create `uq_transactions_external_account` as `CREATE UNIQUE INDEX uq_transactions_external_account ON transactions (external_id, account_id)` — this MUST be a UNIQUE INDEX (not a regular index) to support the ON CONFLICT DO NOTHING deduplication in Plan 02-03. A regular index will cause a runtime error.
       - ENABLE ROW LEVEL SECURITY on all 4 tables
       - For each table, create 4 RLS policies (SELECT, INSERT, UPDATE, DELETE) using: `org_id IN (SELECT public.get_user_org_ids())` — matching exactly the pattern from 00001_foundation.sql
       - For categories: also allow SELECT WHERE org_id IS NULL (system defaults visible to all)
       - INSERT 11 system categories (from research): Salario, Freelance, Investimentos, Aluguel, Alimentacao, Transporte, Saude, Educacao, Lazer, Assinaturas, Outros
       - Use gen_random_uuid() for category IDs, org_id = NULL, is_system = true

    8. Verify full build: `pnpm turbo run build typecheck`
  </action>
  <verify>
    <automated>cd "C:/Users/rdeli/OneDrive/Área de Trabalho/floow" && pnpm --filter @floow/core-finance test && pnpm --filter @floow/db test && pnpm turbo run typecheck</automated>
  </verify>
  <done>SQL migration 00002_finance.sql has all tables, UNIQUE index on (external_id, account_id), RLS policies, and 11 system categories. core-finance package has vitest, balance utilities with passing tests, and type exports. Full monorepo typecheck passes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @floow/db test` passes (schema tests)
- `pnpm --filter @floow/core-finance test` passes (balance tests)
- `pnpm turbo run typecheck` passes (all packages)
- `packages/db/src/schema/finance.ts` exports accounts, transactions, categories, patrimonySnapshots
- `packages/db/src/schema/finance.ts` has uniqueIndex on (externalId, accountId) for transactions
- `packages/db/src/client.ts` includes financeSchema in fullSchema
- `supabase/migrations/00002_finance.sql` exists with RLS + seed + UNIQUE INDEX on external_id+account_id
- `packages/core-finance/src/balance.ts` exports centsToCurrency, currencyToCents
</verification>

<success_criteria>
- Finance schema fully defined with 4 tables, 2 enums, all inferred types
- SQL migration ready with RLS policies matching Phase 1 pattern
- Unique index on (external_id, account_id) in both Drizzle schema and SQL migration
- 11 system categories seeded
- core-finance package bootstrapped with vitest and balance utilities
- All tests pass, typecheck green across monorepo
</success_criteria>

<output>
After completion, create `.planning/phases/02-finance-engine/02-01-SUMMARY.md`
</output>
