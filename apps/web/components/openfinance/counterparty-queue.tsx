// Versão provisória desta task — a Task 9 substitui este arquivo pelo
// conteúdo real (fila de revisão por contraparte).
export function CounterpartyQueue({ orgId, mode }: { orgId: string; mode: 'blocking' | 'page' }) {
  return null
}
