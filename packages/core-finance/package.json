{
  "name": "@floow/core-finance",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "drizzle-orm": "^0.40.0",
    "zod": "catalog:",
    "@floow/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "^3.0.0"
  }
}
